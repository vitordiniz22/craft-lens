{# Section 3: Library Health - always visible #}
<div class="lens-dashboard-section lens-health pane">
    <h2 class="lens-section-header__title">{{ 'Library Health'|t('lens') }}</h2>

    <div class="lens-health__metrics">
        {# Alt Text Coverage #}
        <div class="lens-health__metric">
            <div class="lens-health__metric-header">
                <span class="lens-health__metric-label lens-health__metric-label--lg">{{ 'Alt text coverage'|t('lens') }}</span>
                <span class="lens-health__metric-value {{ altTextCoverage.percentage >= 80 ? 'lens-health__metric-value--good' : (altTextCoverage.percentage >= 50 ? '' : 'lens-health__metric-value--warning') }}">
                    {{ altTextCoverage.percentage }}%
                </span>
            </div>
            <div class="lens-health__metric-detail">
                <span class="lens-health__metric-counts">({{ altTextCoverage.withAltText }} {{ 'of'|t('lens') }} {{ altTextCoverage.total }})</span>
                {% if altTextCoverage.total - altTextCoverage.withAltText > 0 %}
                    <a href="{{ cpUrl('lens/search', {missingAltText: 1}) }}" class="lens-health__metric-link">
                        {{ 'View missing'|t('lens') }} &rarr;
                    </a>
                {% endif %}
            </div>
        </div>

        {# Tagged Assets #}
        <div class="lens-health__metric">
            <div class="lens-health__metric-header">
                <span class="lens-health__metric-label lens-health__metric-label--lg">{{ 'With tags'|t('lens') }}</span>
                <span class="lens-health__metric-value">{{ taggedPercentage.percentage }}%</span>
            </div>
            <div class="lens-health__metric-detail">
                <span class="lens-health__metric-counts">({{ taggedPercentage.withTags }} {{ 'of'|t('lens') }} {{ taggedPercentage.total }})</span>
            </div>
        </div>

        {# High Quality #}
        <div class="lens-health__metric">
            <div class="lens-health__metric-header">
                <span class="lens-health__metric-label lens-health__metric-label--lg">{{ 'High quality'|t('lens') }}</span>
                <span class="lens-health__metric-value {{ highQualityPercentage.percentage >= 70 ? 'lens-health__metric-value--good' : (highQualityPercentage.percentage >= 40 ? '' : 'lens-health__metric-value--warning') }}">
                    {{ highQualityPercentage.percentage }}%
                </span>
            </div>
            <div class="lens-health__metric-detail">
                <span class="lens-health__metric-counts">({{ highQualityPercentage.highQuality }} {{ 'of'|t('lens') }} {{ highQualityPercentage.total }})</span>
                {% if highQualityPercentage.total - highQualityPercentage.highQuality > 0 %}
                    <a href="{{ cpUrl('lens/search', {qualityBelow: '0.7'}) }}" class="lens-health__metric-link">
                        {{ 'View low quality'|t('lens') }} &rarr;
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
