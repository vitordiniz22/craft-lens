{% extends '_layouts/cp' %}

{% set title = 'Bulk Processing'|t('lens') %}
{% set selectedSubnavItem = 'bulk' %}

{% block content %}
    {% set isConfigError = state == 'complete' and failureReason is defined and failureReason.message and failureReason.isConfigError %}

    <div class="lens-bulk-page">

        {# Stats Grid - Hidden when config error caused processing to fail #}
        {% if not isConfigError %}
            <div class="lens-stats-grid">
                <div class="lens-stat-card lens-stat-neutral" data-lens-stat="totalImages" data-lens-count="{{ stats.totalImages }}">
                    <div class="lens-stat-value">{{ stats.totalImages }}</div>
                    <div class="lens-stat-label">{{ 'Total Images'|t('lens') }}</div>
                </div>
                <div class="lens-stat-card lens-stat-success" data-lens-stat="analyzed" data-lens-count="{{ stats.analyzed }}">
                    <div class="lens-stat-value">{{ stats.analyzed }}</div>
                    <div class="lens-stat-label">{{ 'Analyzed'|t('lens') }}</div>
                </div>
                <div class="lens-stat-card lens-stat-warning" data-lens-stat="unprocessed" data-lens-count="{{ stats.unprocessed }}">
                    <div class="lens-stat-value">{{ stats.unprocessed }}</div>
                    <div class="lens-stat-label">{{ 'Unprocessed'|t('lens') }}</div>
                </div>
                <div class="lens-stat-card lens-stat-info" data-lens-stat="pendingReview" data-lens-count="{{ stats.pendingReview }}">
                    <div class="lens-stat-value">{{ stats.pendingReview }}</div>
                    <div class="lens-stat-label">{{ 'Pending Review'|t('lens') }}</div>
                </div>
                <div class="lens-stat-card lens-stat-error" data-lens-stat="failed" data-lens-count="{{ stats.failed }}">
                    <div class="lens-stat-value">{{ stats.failed }}</div>
                    <div class="lens-stat-label">{{ 'Failed'|t('lens') }}</div>
                </div>
            </div>
        {% endif %}

        {# Render only the current state #}
        {% if state == 'processing' %}
            {% include 'lens/_bulk/_state-processing' %}
        {% elseif state == 'complete' %}
            {% include 'lens/_bulk/_state-complete' %}
        {% else %}
            {% include 'lens/_bulk/_state-ready' %}
        {% endif %}

    </div>
{% endblock %}
