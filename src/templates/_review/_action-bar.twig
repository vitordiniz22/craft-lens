{#
 # Review Action Bar — Approve/Reject navigation controls
 #
 # Expected variables:
 #   prevId - int|null, previous analysis ID
 #   nextId - int|null, next analysis ID
 #}
<div class="lens-review-action-bar">
    {% if prevId %}
        <a href="{{ cpUrl('lens/review/' ~ prevId) }}" class="btn" data-lens-action="review-prev">
            <kbd>&larr;</kbd> {{ 'Previous'|t('lens') }}
        </a>
    {% endif %}

    <button type="submit" class="btn submit" data-lens-action="review-approve">
        {{ 'Approve & Next'|t('lens') }} <kbd>A</kbd>
    </button>

    <button type="submit" class="btn"
            formaction="{{ actionUrl('lens/review/reject') }}"
            data-lens-action="review-reject">
        {{ 'Reject'|t('lens') }} <kbd>R</kbd>
    </button>

    {% if nextId %}
        <a href="{{ cpUrl('lens/review/' ~ nextId) }}" class="btn" data-lens-action="review-next">
            {{ 'Next'|t('lens') }} <kbd>&rarr;</kbd>
        </a>
    {% endif %}
</div>
